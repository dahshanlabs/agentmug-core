---
"@agentmug/runtime": minor
---

Export the portable `request_capability` host contract so authenticated workers can suggest an owner-reviewed capability build without starting code generation or mutating the worker.
